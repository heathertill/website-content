import * as React from 'react';

export interface LandingInfoProps { }

const LandingInfo: React.SFC<LandingInfoProps> = () => {
    return (
        <div>Hello from the landing page</div>
    );
}

export default LandingInfo;